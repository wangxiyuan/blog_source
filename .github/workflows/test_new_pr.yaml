name: Test PR

on: [pull_request]

jobs:
  test_pr:
    name: Generate page
    runs-on: ubuntu-latest
    steps:
      - name: Install requirements
        run: |
          apt install -y nodejs npm
          npm install -g hexo-cli
      - name: Generate page
        run: |
          hexo clean
          hexo generate
